#!/bin/bash
#The first line of the file should contain the #!/path/to/shell.
#Each shell behaves slightly differently so it is good practice to make sure
#that the script runs within the expected shell it was written for.

#The default echo command takes a string (or variable) and prints the contents to the console.
echo "Hello World"
