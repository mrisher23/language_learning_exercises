
-- Uses the io library to write a string to standard out.
-- The global variable _VERSION provides the version number of Lua being used.
-- The commas around _VERSION concatenate the string together
io.write("Hello world, from ",_VERSION,"!\n")